# تعديلات مشروع "meters" — الأدوار الثلاثة + الدرور + عارض البيانات

## ما الذي تغيّر

### 1) نظام الأدوار الثلاثة (`lib/core/models/user_model.dart`)
تمت إضافة `enum AppRole { admin, dataEntry, viewer }` إلى جانب القطاعات
الموجودة أصلاً (`UserDepartment`). كل مستخدم الآن يملك:

- `appRole` → الدور الرئيسي (أدمن / مدخل بيانات / عارض بيانات).
- `sectors` → قائمة القطاعات المسموح له بالوصول إليها (تفرّع الرول: قد
  تكون قطاعاً واحداً، أو أكثر من قطاع لنفس المستخدم).
- `accessibleSectors` → getter يُرجع كل القطاعات تلقائياً للأدمن، أو قائمة
  `sectors` لأي مستخدم آخر.
- `canSwitchModes` → صحيح فقط للأدمن، ويتحكم بظهور مبدّل
  "إدخال/عرض" في الدرور.

تمت إضافة 8 حسابات تجريبية (`kMockUsers`) تغطي كل الحالات: أدمن، مدخل/عارض
بيانات لكل قطاع على حدة، وعارض بيانات مسؤول عن أكثر من قطاع
(`multi.view@energy.gov.sy` → المياه + الكهرباء). كلمة المرور لكل
الحسابات: `123456`.

### 2) الدرور بدل شريط التنقّل (`lib/core/widgets/app_drawer.dart`)
تمت إزالة الـ Bottom Navigation Bar بالكامل. الدرور الجديد يعرض:
- رأس بمعلومات المستخدم (الاسم، المسمّى الوظيفي، شارة الدور، شارة القطاع).
- مبدّل "إدخال البيانات / عرض البيانات" — يظهر **فقط** للأدمن.
- قائمة القطاعات المسموح بها فعلياً لهذا المستخدم فقط.
- زر تسجيل الخروج.

### 3) `MainShell` (`lib/features/main/presentation/screens/main_shell.dart`)
أصبح يدير حالتين: القطاع النشط (`activeSector`) ووضع العمل (`mode`)، ويبني
واجهة الإدخال أو واجهة العرض حسب الوضع الحالي عبر `sector_registry.dart`.
شريط التطبيق العلوي يتحوّل تلقائياً إلى التدرّج **الخمري** في وضع العرض،
وإلى التدرّج الأخضر الغامق الأصلي في وضع الإدخال.

### 4) لون خمري جديد في الهوية البصرية (`lib/core/theme/app_theme.dart`)
أُضيفت مجموعة ألوان `burgundy*` وتدرّج `burgundyHeaderGradient` يُستخدمان
حصراً لتمييز وضع "عرض البيانات" بصرياً عن وضع "إدخال البيانات"، دون
المساس بأي لون أو مكوّن موجود مسبقاً.

### 5) رول "عارض البيانات" الجديد (`lib/features/viewer/`)
وحدة كاملة جديدة تحاكي لوحات الويب المرفقة، لكن بتصميم موبايل-فرست:

- `data/viewer_models.dart` — النماذج (`SectorViewerConfig`, `KpiItem`,
  `GeoStation`, `BarDatum`, `TopRow`, `PlanRow`, `SeasonalSplit`).
- `data/viewer_sector_configs.dart` — بيانات تجريبية مطابقة لمحتوى الصور
  المرفقة (المياه: تحليل الهطول، النفط والغاز: التقرير اليومي، الكهرباء:
  التوليد والشبكة).
- `data/viewer_repository.dart` — طبقة وصول بيانات جاهزة للربط بـ API عبر
  `Dio` (المستخدم أصلاً في `pubspec.yaml`)؛ حالياً تُرجع بيانات محلية، مع
  تعليق `TODO(API)` يوضّح بالضبط كيف تُستبدل بنداء حقيقي دون تغيير أي
  واجهة.
- `presentation/screens/sector_viewer_screen.dart` — الشاشة العامة
  (تُستخدم لكل القطاعات الثلاثة عبر إعداد مختلف): مؤشرات KPI، استقراءات،
  خريطة تفاعلية، مخططات مقارنة/شهرية/سنوية، جدول "أعلى عناصر"، وأقسام
  اختيارية (الملف الموسمي للمياه، الخطة الشهرية للنفط والغاز).
- `presentation/widgets/` — كل قسم في بطاقة مستقلة قابلة لإعادة الاستخدام:
  `viewer_kpi_grid`, `viewer_common` (استقراءات + إطار بطاقة موحّد),
  `viewer_map_card` (خريطة **flutter_map** حقيقية فوق OpenStreetMap مع
  دوائر متدرّجة الحجم/اللون وطبقات قابلة للتبديل للكهرباء)،
  `viewer_bar_chart_card` و `viewer_seasonal_donut` (مخططات **fl_chart**
  الأعمدة/الدائرية)، `viewer_top_rows_card`، `viewer_plans_card`.

### 6) حزم جديدة في `pubspec.yaml`
تمت إضافة `flutter_map` و `latlong2` لعرض الخرائط التفاعلية. مخططات
`fl_chart` كانت موجودة أصلاً في الاعتماديات ولم تُستخدم من قبل — أصبحت
الآن مُفعّلة فعلياً في وحدة العرض.

### 7) الصفحة الرئيسية (`lib/features/home/presentation/screens/home_screen.dart`)
تعرض الآن فقط القطاعات المسموح بها للمستخدم الحالي (بدل عرض الثلاثة
دائماً)، مع شريط يوضّح وضع العمل الحالي (إدخال/عرض).

### 8) تبويبات قطاع المياه الثلاثة + إعادة بناء الكهرباء بالكامل
- **`WaterViewerScreen`** (جديدة) تضيف تبويباً داخلياً بثلاث شاشات مطابقة
  لنسخة الويب تماماً: **تحليل الهطول** (كما كانت)، **معلومات السدود**
  (جديدة — كل الأرقام والاستقراءات والخريطة الفئوية والجدول مأخوذة حرفياً
  من لقطة الشاشة)، **محطات مياه الشرب** (جديدة).
- **لوحة الكهرباء** أُعيد بناؤها بالكامل لتطابق شاشة "التقرير اليومي" بكل
  أقسامها: مجموعات "معلومات / استهلاك المحافظات / التوليد والتزويد /
  الحوادث والتعطيل" (`InfoTileGroup`)، دونات "مزيج التوليد"، جدولا "حالة
  مجموعات التوليد" و"المجموعات تحت الصيانة"، سجلّا "الحوادث" و"خطوط
  النقل"، وشبكة مخططات اتجاه (Sparklines) لكل مؤشر.
- أُضيفت نماذج بيانات عامة جديدة قابلة لإعادة الاستخدام في أي قطاع مستقبلاً:
  `InfoTile/InfoTileGroup`، `DonutSegment/MultiDonutConfig`,
  `PercentDonutConfig`، `LineSeriesConfig`، `GenericStatusTable`،
  `LogSection`، `MapLegendCategory` (خريطة بمفتاح فئوي بدل تدرّج لوني).

> **ملاحظة شفافية:** تبويب "محطات مياه الشرب" في نسخة الويب كثيف جداً
> (شبكة من +14 دونات صغير). المؤشرات الخمسة العلوية والخريطة نُقلت
> بدقّة من لقطة الشاشة، أما بعض الدونتس الصغيرة جداً في الشبكة الكثيفة
> فبعض أرقامها قد تحتاج تأكيداً إضافياً لأن النص كان صغيراً جداً على
> الصورة المصغّرة — البنية والتجميع مطابقان، والقيم مقاربة معقولة. أرسل
> لقطة أوضح/مقرّبة لهذا القسم إذا احتجت تطابقاً رقمياً حرفياً 100%.

### 9) مطابقة دقيقة لتقارير النفط والغاز والكهرباء (تصحيح شامل)
بعد مراجعة لقطات شاشة إضافية، تبيّن أن تصميمَي لوحتي **النفط والغاز**
و**الكهرباء** السابقين لم يطابقا الشكل الحقيقي لتقاريرهما اليومية. أُعيد
بناء الإعدادين بالكامل (`oilGasViewerConfig` و `electricityViewerConfig`
في `viewer_sector_configs.dart`) بحيث تُطابق كل قيمة ظاهرة في اللقطات:

- **النفط والغاز**: تحوّلت مؤشرات KPI الأربعة السابقة (غير الصحيحة) إلى
  4 مجموعات `InfoTileGroup` مطابقة حرفياً: **إنتاج النفط** (98.512 /
  106 bbl)، **الغاز النظيف** (6 مؤشرات: الاستهلاك، التوزيع، الإجمالي،
  المستورد من الأردن/أذربيجان، الإنتاج المحلي)، **مبيعات المشتقات** (فيول،
  غاز مسال، بنزين، مازوت)، **أسعار السلع** (غاز MMBtu، نفط برنت). أُضيف
  تنبيه "تنبيهات حرجة: لا توجد تنبيهات نشطة" ومخططا "توزيع الغاز النظيف"
  و"مزيج الغاز النظيف" و"مبيعات المشتقات"، إضافة لشبكة مخططات اتجاه (14
  يوماً) لكل من: مجموع الغاز الموزَّع، إجمالي الغاز، إجمالي إنتاج النفط،
  استهلاك قطاع الكهرباء من الغاز.
- **الكهرباء**: أُعيدت المؤشرات بالكامل كمجموعات `InfoTileGroup` تطابق
  تبويب "معلومات" (20 مؤشراً)، "استهلاك المحافظات" (3 مؤشرات: التجاوز،
  المخصصة، المستجرة)، "التوليد والتزويد" (15 مؤشراً)، و"الحوادث والتشغيل"
  (حوادث الخطوط 4، حوادث التوليد 6). أُضيف تنبيه علوي "التنبيهات" بنفس
  نص اللقطة، دونات "مزيج التوليد"، مخطط أعمدة مزدوج جديد **"تحليل
  المحافظة"** (الطاقة المستهلكة/المخصصة — نموذج `DualBarDatum` الجديد)،
  جدولا حالة حقيقيان **"حالة مجموعات التوليد"** و**"المجموعات تحت
  الصيانة"** (بانياس/محردة، aggregate)، سجلّا **"سجل الحوادث"** بنص كل
  حادثة كما ظهر بالضبط (فصل/وصل محوّلات وخطوط)، وشبكة مخططات اتجاه (11
  قراءة) لثماني سلاسل: التوليد البخاري، التوليد الصافي، ذروة التوليد،
  الاستطاعة المتاحة، الغاز الوارد، نسبة المخزون، المخزون القابل
  للاستهلاك، والتجاوز.
- لا خريطة في هاتين اللوحتين (تُخفى تلقائياً الآن عبر شرط
  `if (cfg.stations.isNotEmpty)` في `ViewerAsyncBody`) لأن التقارير
  المرجعية لا تحتوي خريطة لهذين القطاعين.
- تمت إضافة حقلين عامّين جديدين لإعادة الاستخدام: `insightsTitle` /
  `insightsIcon` (لتخصيص عنوان/أيقونة بطاقة "استقراءات" — مثل "التنبيهات"
  أو "تنبيهات حرجة" بدل العنوان الافتراضي)، و`DualBarDatum` +
  `ViewerDualBarChartCard` (مخطط أعمدة بسلسلتين جنباً إلى جنب).
- تم تصحيح ترتيب عرض الأقسام في `ViewerAsyncBody` ليطابق تسلسل الشاشات
  الحقيقية: معلومات → تنبيهات → (خريطة إن وُجدت) → مخططات مقارنة/دونتس →
  جداول حالة → سجلّات حوادث → شبكة مخططات الاتجاه → الخطط/الجداول.

### 10) قسمان داخل تبويب "معلومات السدود" (نظرة عامة + سلسلة سدود الفرات)
أُضيف تبديل داخلي (Segmented control) أعلى تبويب "معلومات السدود" بقسمين
مطابقين تماماً لنسخة الويب:

- **"نظرة عامة"**: اللوحة التنفيذية العامة الموجودة أصلاً لكل سدود المياه.
- **"سلسلة سدود الفرات"** (جديدة، شاشة `EuphratesSeriesScreen`): عرض
  تفصيلي مخصّص لسلسلة سدود الفرات (تشرين، الفرات/طبقة، كديران) — منقول
  حرفياً من لقطة الشاشة المرجعية لشهر نيسان 2026:
  - رأس بمنتقي الشهر + 5 مؤشرات KPI (تخزين السلسلة، التوليد اليومي،
    منسوب تشرين، منسوب الفرات، وارد جرابلس).
  - 3 مخططات "التحليل الشهري" بسلسلة واحدة لكل منها (التخزين الإجمالي،
    التوليد الكهربائي اليومي، الوارد المائي) مع صف إحصاءات (بداية/نهاية
    الشهر، المتوسط/الذروة/الأدنى) أسفل كل مخطط.
  - بطاقة "استقراءات" بالنص الحرفي من الصورة (6 نقاط).
  - 3 بطاقات "تفاصيل تشغيلية" لكل سد (كديران، الفرات/طبقة، تشرين) بكل
    قيمها (المنسوب، المخزون، الممرر، التوليد، نسبة الامتلاء).
  - 3 مخططات خطية متعددة السلاسل (حجم التخزين، الطاقة المتولدة يومياً،
    منسوب المياه) مع مفتاح ألوان لكل سد.
  - جدول "القراءات اليومية" الكامل (11 عموداً × 15 صفاً المتاحة في
    الصورة) بتصفّح صفحات، قابل للتمرير أفقياً على الموبايل حفاظاً على
    دقّة كل عمود.

> **ملاحظة شفافية:** كل الأرقام أعلاه (المؤشرات، إحصاءات المخططات
> الثلاثة الأولى، الاستقراءات، بطاقات السدود الثلاث، وجدول القراءات
> اليومية بكامله) مطابقة حرفياً للصورة. أما نقاط البيانات الداخلية لكل
> يوم في المخططات الثلاثة **متعددة السلاسل** فقط (حجم التخزين / الطاقة
> المتولدة يومياً / منسوب المياه) فقُدّرت بصرياً من شكل الخطوط لأن الصورة
> لا تحمل قيماً رقمية على كل نقطة لهذه المخططات الثلاثة تحديداً — الشكل
> العام والاتجاه مطابقان. كما أن جدول القراءات يحتوي 15 صفاً فقط (الصفحة
> الأولى المعروضة في الصورة)؛ العنوان الأصلي يذكر "من 30" صفاً، فأضفت
> تصفّح صفحات جاهزاً لإضافة الصفوف المتبقية لاحقاً عند توفّرها.

### 11) إعادة بناء شاملة لتبويب "محطات مياه الشرب" (مطابقة كاملة)
راجعت لقطة شاشة كاملة الطول لهذا التبويب ولاحظت أقساماً كثيرة كانت
مفقودة كلياً من النسخة السابقة. أعدت بناء `waterDrinkingViewerConfig`
بالكامل لتغطية **كل** الأقسام الظاهرة بالصورة، وأضفت 3 مكوّنات عامة جديدة
قابلة لإعادة الاستخدام في أي قطاع مستقبلاً لتحقيق ذلك:

- **`HorizontalBarConfig` + `ViewerHorizontalBarCard`**: مخطط أعمدة أفقي
  (شريط تقدّم لكل فئة) — يُستخدم لثلاثة أقسام كانت مفقودة: "سبب عدم توصيل
  المياه"، "أسباب عدم التوصيل حسب المحافظة"، و"التوزيع الجغرافي حسب
  المحافظة".
- **`DualBarSection` + إعادة استخدام `ViewerDualBarChartCard`**: يسمح
  بتكرار عدة مخططات "حسب المحافظة" (نعم/لا) ضمن شاشة واحدة — أضفت 4
  مخططات كانت مفقودة: "هل المحطة تعمل حالياً حسب المحافظة"، "إمداد الشبكة
  حسب المحافظة"، "توصيل الشبكة حسب المحافظة"، "إضاءة الطاقة الشمسية حسب
  المحافظة".
- **حقول `opTableTitle/opTableColumns/opTableRows` + إعادة استخدام
  `ViewerReadingsTable`**: أضفت جدول "البيانات التشغيلية" الكامل (كان
  مفقوداً بالكامل) بأعمدته الستة (الرقم، التسمية، المحافظة، الوحدة/المجمع،
  العنوان، الحالة) مع تصفّح صفحات وتمرير أفقي.
- وسّعت شبكة الدونتس (`multiDonuts`) لتغطي فعلياً قسم "مؤشرات التشغيل
  للمحطات" الستة (نوع المحطة: بئر/ضخ/تنقية، هل تعمل، مصدر المياه، التوصيل
  الكهربائي) بعناوين أدق من السابق.
- أعدت استخدام حقلي `monthlyTitle/monthlyBars` و`yearlyTitle/yearlyBars`
  الموجودين أصلاً (بعناوين جديدة "إضاءة الطاقة الشمسية" و"إضاءة الطاقة
  التحسينية") بدل تركهما فارغين، لأنهما يطابقان مخططين صاعدين صغيرين في
  الصورة كانا مفقودين.

> **ملاحظة شفافية:** المؤشرات الخمسة العلوية، الخريطة، عناوين كل الأقسام،
> وبنية كل رسم (نوعه وترتيبه) مطابقة تماماً لما هو ظاهر بالصورة. أرقام
> بعض الدونتس الصغيرة جداً، فئات المخطط الأفقي "سبب عدم توصيل المياه"،
> وصفوف جدول "البيانات التشغيلية" قُدّرت بأفضل قراءة ممكنة من الصورة نظراً
> لصغر الخط في تلك المناطق تحديداً وتشابه أسماء المدن مع بيانات عيّنة/حشو
> ظاهرة بالجدول الأصلي — البنية والتجميع (عدد الأقسام وترتيبها ونوع كل
> رسم) مطابق 100%، وبإمكانك تزويدي بلقطة مقرّبة لأي قسم إذا احتجت تطابقاً
> رقمياً حرفياً 100% فيه.

## كيفية التشغيل
```bash
flutter pub get
flutter run
```

## حسابات تجريبية سريعة (كلمة المرور لكل الحسابات: 123456)
| البريد الإلكتروني              | الدور            | القطاع/القطاعات        |
|--------------------------------|------------------|--------------------------|
| admin@energy.gov.sy            | مدير النظام      | كل القطاعات + تبديل الوضع|
| water.entry@energy.gov.sy      | مدخل بيانات      | المياه                   |
| water.view@energy.gov.sy       | عارض بيانات      | المياه                   |
| oil.entry@energy.gov.sy        | مدخل بيانات      | النفط والغاز             |
| oil.view@energy.gov.sy         | عارض بيانات      | النفط والغاز             |
| elec.entry@energy.gov.sy       | مدخل بيانات      | الكهرباء                 |
| elec.view@energy.gov.sy        | عارض بيانات      | الكهرباء                 |
| multi.view@energy.gov.sy       | عارض بيانات      | المياه + الكهرباء        |

## نقطة ربط الـ API لاحقاً
كل ما يلزم عند تجهيز الخادم الفعلي هو تعديل دالة واحدة:
`ViewerRepository.fetchSectorData()` في
`lib/features/viewer/data/viewer_repository.dart` — استبدال المحاكاة
بنداء `Dio` حقيقي يُرجع نفس شكل `SectorViewerConfig`. لا حاجة لتعديل أي
شاشة أو ويدجت عرض.
